# Base Builder Code (Base.dev → your project → Builder Code).
# When set, every wagmi transaction from this app carries the ERC-8021
# attribution suffix, so Base credits onchain activity to your app.
# Build it in:  VITE_BUILDER_CODE=abcd npm run build     (or put it in .env.local)
VITE_BUILDER_CODE=
